//Language Action Generators

//Swap Language

/**
 * swapLanguage - Generates a command keyword so the central controller can change the language of the application's user interface. 
 */
export const swapLanguage = () => ({
    type: 'SWAP_LANGUAGE'
});