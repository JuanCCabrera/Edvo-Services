//Failure Modal Action Generators

/**
 * setFailureModal - Generates a command keyword so the central controller to toggle the Failure modal.  
 */
export const setFailureModal = () => ({
    type: 'TOGGLE_FAILURE_MODAL'
});