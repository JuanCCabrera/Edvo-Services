//Success Modal Action Generators

/**
 * setSuccessModal - Generates a command keyword so the central controller to toggle the Success modal.  
 */
export const setSuccessModal = () => ({
    type: 'TOGGLE_SUCCESS_MODAL'
});