//Success Modal Action Generators

/**
 * setEditModal - Generates a command keyword so the central controller to toggle the Edit modal.  
 */
export const setEditModal = () => ({
    type: 'TOGGLE_EDIT_MODAL'
});