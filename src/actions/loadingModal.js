//Loading Modal Action Generators

/**
 * setLoadingModal - Generates a command keyword so the central controller to toggle the Loading modal.  
 */
export const setLoadingModal = () => ({
    type: 'TOGGLE_LOADING_MODAL'
});